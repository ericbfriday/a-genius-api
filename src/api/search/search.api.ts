/**
 * ## Search
 *
 * The search capability covers all content hosted on Genius (all songs).
 */
export class GeniusApiSearch {
	/**
	 * Search documents hosted on Genius.
	 *
	 * `GET /search`
	 *
	 * @param searchTerm - The term to search for.
	 */
	public search = (searchTerm: string) => {
		/** do stuff */
	}
}
